<% @title = "Home" %>

<h2 class="page-title">scripty2 API</h2>

<div class="page-introduction">
  <p>Welcome to the scripty2 API Documentation.</p>
  
  <p>
    At the moment this complete rewrite of the venerable script.aculo.us
    library is a prerelease alpha version and not meant for production use (although we used it
    in <a href="http://twistori.com/">live projects</a> and it worked great!).
  </p>
  
  <p>
    scripty2 is divided into several parts, as follows.
  </p>
</div>


<div class="section section-sections">
  <div class="section-title">
    <h3>Sections</h3>
  </div>
  
  <div class="section-content">
    <ul class="section-list">
      <% @root.sections.each do |section| %>
      <li>
        <h4>
          <a href="<%= path_to_section(section) %>"><%= section.name %></a>
        </h4>
        <p><%= htmlize(section.short_description) %></p>
      </li>      
      <% end %>
    </ul>
  </div>
</div>